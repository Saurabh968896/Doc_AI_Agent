version: "3.9"

services:

  app:
    build: .
    container_name: ai-agent-app
    ports:
      - "8000:8000"
    depends_on:
      - redis
    volumes:
      - .:/app

  redis:
    image: redis:latest
    container_name: redis-server
    ports:
      - "6379:6379"